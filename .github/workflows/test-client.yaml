name: client tests

on:
  pull_request:
    branches:
      - main
      - dev
  workflow_dispatch:

jobs:
  test-client-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./Client

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build docker image
        run: timeout 5m docker build -t c_snakes .

      - name: Cleanup docker
        run: docker system prune -f
